import { HomePage } from '../../components/home/HomePage';

// first entry point is to fetch and load data and show all loadings here
const Home = () => {
  return (
    <div>
      <HomePage />
    </div>
  );
};
export default Home;
